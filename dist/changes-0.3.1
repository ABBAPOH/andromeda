The Andromeda 0.3.1 release contains bug fixes and new features.

The most important changes are listed in this document. For a complete
list of changes, see the Git log for the Andromeda sources that
you can check out from the public Git repository. For example:

git clone git@github.com:ABBAPOH/Andromeda.git
git log --cherry-pick --pretty=oneline v0.3.0..v0.3.1

Global
   * Migrate from CMake to QBS

BinEditor plugin

Bookmarks plugin

FileManager plugin
   * Enable Open With menu in release
   * Fix FileDelegate wrapping and eliding
   * Fix Column View mode in case when files should be opened by 1-click

Image Viewer plugin

PdfView plugin

TextEditor plugin

Hello World plugin

WebView plugin
